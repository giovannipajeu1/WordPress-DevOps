pipeline {
    agent any

    stages {
        stage('Build Docker Compose') {
            steps {
                // Buildar o Docker Compose
               
            }
        }
        stage('DAST Pipeline with OWASP ZAP') {
            steps {
                // Executar a pipeline DAST com OWASP ZAP
                
            }
        }
    }
}
